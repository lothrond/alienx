---
# (These are wrapped by the makefile)
driver_stack: "__DRIVER_STACK__"          # nouveau | nvidia
session_type: "__SESSION_TYPE__"          # wayland | x11
bluray_support: "__BLURAY_SUPPORT__"      # true | false
native_steam: "__NATIVE_STEAM__"          # true | false
browser: "__BROWSER__"                    # firefox | chrome | ...
cockpit_port: "__COCKPIT_PORT__"          # 9090 or custom
firewall_enabled: "__FIREWALL_ENABLED__"  # true | false
username: "__USER_GAME__"
hostname: "__LOCAL_HOST__"